<%= form_for(@card_type) do |f| %>
  <% if @card_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@card_type.errors.count, "error") %> prohibited this card_type from being saved:</h2>

      <ul>
      <% @card_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :note_id %><br>
    <%= f.number_field :note_id %>
  </div>
  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :front_template %><br>
    <%= f.text_area :front_template %>
  </div>
  <div class="field">
    <%= f.label :back_template %><br>
    <%= f.text_area :back_template %>
  </div>
  <div class="field">
    <%= f.label :styling %><br>
    <%= f.text_area :styling %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
